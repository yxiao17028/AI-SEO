document.querySelectorAll('.chip[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.finding').forEach(row=>row.style.display=(btn.dataset.filter==='all'||row.dataset.status===btn.dataset.filter)?'grid':'none')}));
const provider=document.getElementById('provider');if(provider&&window.PROVIDERS){const model=document.getElementById('model'),note=document.getElementById('provider-note'),key=document.getElementById('api-key');function sync(){const p=window.PROVIDERS[provider.value];model.value=p.default_model;note.textContent=p.note;key.placeholder=provider.value==='demo'?'Not required for Demo Mode':'Enter your provider API key';}provider.addEventListener('change',sync);sync();document.getElementById('test-connection').addEventListener('click',async()=>{const status=document.getElementById('connection-status');status.textContent=' Testing...';try{const r=await fetch('/api/ai/test-connection',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({provider:provider.value,model:model.value,api_key:key.value})});const d=await r.json();status.textContent=(d.success?' ✓ ':' ✕ ')+d.message;status.style.color=d.success?'#16745a':'#a63131'}catch(e){status.textContent=' ✕ Connection test failed.'}})}
